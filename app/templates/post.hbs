
<!-- <button {{action 'sayHelo'}}> Say </button> -->

<h1>Hello</h1>

<h3>{{title}}</h3>
{{boby}}

<!-- {{#each authors}}
  {{this}},
{{/each}} -->

<br>

<h2>Comments</h2>
<!-- <ul>
  {{#each comments as comment}}
    <li>{{name}}</li>
  {{/each}}
</ul> -->


{{#each model as |post|}}
  <article class="listing">
    <h3>{{post.title}}</h3>
    <div class="detail owner">
      <span>Owner:</span> {{rental.owner}}
    </div>
    <div class="detail type">
      <span>Type:</span> {{post.type}}
    </div>
    <div class="detail location">
      <span>Location:</span> {{post.city}}
    </div>
    <div class="detail bedrooms">
      <span>Number of bedrooms:</span> {{post.bedrooms}}
    </div>
  </article>
{{/each}}
